// Get the checkbox and body element
const toggleSlider = document.getElementById('toggle-slider');
const body = document.body;
const paragraph = document.querySelector('p');

// Set initial mode based on user's preference or default to dark mode
if (localStorage.getItem('theme') === 'light') {
    body.classList.remove('dark-mode');
    body.classList.add('light-mode');
    paragraph.classList.add('light-mode');
    toggleSlider.checked = true; // Set slider to checked
} else {
    body.classList.add('dark-mode');
    paragraph.classList.add('dark-mode');
    toggleSlider.checked = false; // Set slider to unchecked
}

// Toggle function
toggleSlider.addEventListener('change', () => {
    if (toggleSlider.checked) {
        body.classList.remove('dark-mode');
        body.classList.add('light-mode');
        paragraph.classList.remove('dark-mode');
        paragraph.classList.add('light-mode');
        localStorage.setItem('theme', 'light'); // Store preference in local storage
    } else {
        body.classList.remove('light-mode');
        body.classList.add('dark-mode');
        paragraph.classList.remove('light-mode');
        paragraph.classList.add('dark-mode');
        localStorage.setItem('theme', 'dark'); // Store preference in local storage
    }
});
/* General body styling (Dark Mode with uniform gradient background) */
body {
    font-family: 'Roboto', sans-serif; /* Updated font */
    background: linear-gradient(100deg, #000000, #463535, #5a3131, #7e0d0d);
    background-size: cover; /* Ensures the gradient covers the entire viewport */
    background-repeat: no-repeat; /* Prevents any repetition */
    background-attachment: fixed; /* Makes sure gradient stays fixed on scroll */
    color: #e0e0e0; /* Light color for better contrast */
    margin: 0;
    padding: 0;
    transition: background 0.3s, color 0.3s;
}

/* Light Mode Styles */
body.light-mode {
    background: linear-gradient(100deg, #ffffff, #f0f4f8, #d6e8ff, #b3d9ff);
    color: #333; /* Dark text for light mode */
}

/* Header styling */
h1, h2, h3, h4, h5, h6 {
    margin: 20px 0; /* Add margin for spacing */
    padding: 0; /* Reset padding */
    text-align: center; /* Center align */
}

/* Paragraph styling with gradient shading */
p {
    font-size: 18px;
    text-align: center;
    padding: 20px;
    border: none; /* Removed border */
    max-width: 500px;
    margin: 30px auto;
    border-radius: 15px; /* More rounded corners */
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1); /* Subtle shadow */
    transition: background 0.3s, color 0.3s;
}

/* Light Mode Paragraph */
body.light-mode p {
    background: linear-gradient(135deg, #ffffff, #d0dcff, #6bfff8, #00fff2); /* Light gradient */
    color: #555; /* Darker text color for readability */
}

/* Dark Mode Paragraph */
body.dark-mode p {
    background: linear-gradient(135deg, #3b3131, #614040, #7c3a3a, #940a0a); /* Dark gradient */
    color: #fffff4; /* Light text color */
}

/* Toggle Switch Styles */
.switch {
    position: relative;
    display: inline-block;
    width: 50px; /* Wider for easier toggling */
    height: 25px;
    margin: 20px auto; /* Center the switch */
}

.switch input {
    opacity: 0; /* Hide the default checkbox */
    width: 0;
    height: 0;
}

.slider {
    position: absolute;
    cursor: pointer;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-color: #ccc;
    transition: .4s;
    border-radius: 20px; /* Rounded corners */
}

.slider:before {
    position: absolute;
    content: "";
    height: 20px;
    width: 20px;
    left: 2px; /* Adjust for aesthetics */
    bottom: 2px;
    background-color: #fff; /* White toggle */
    transition: .4s;
    border-radius: 50%; /* Round toggle */
}

/* Checkbox checked styles */
input:checked + .slider {
    background-color: #dbcf22; /* Color when checked */
}

input:checked + .slider:before {
    transform: translateX(25px); /* Move toggle to the right */
}

/* Button Styles */
.theme-button {
    font-size: 14px; /* Smaller font size */
    padding: 10px 15px; /* Padding for the button */
    border: none; /* Remove default border */
    border-radius: 20px; /* More rounded corners */
    cursor: pointer; /* Pointer cursor on hover */
    transition: background-color 0.3s, color 0.3s, transform 0.3s; /* Smooth transition */
    font-weight: bold; /* Bold text */
}

/* Light Mode Button */
body.light-mode .theme-button {
    background: linear-gradient(135deg, #e0e0e0, #f4f4f4); /* Light gradient */
    color: #333; /* Dark text color */
}

/* Dark Mode Button */
body.dark-mode .theme-button {
    background: linear-gradient(135deg, #444, #666); /* Dark gradient */
    color: #f4f4f4; /* Light text color */
}

/* Button Hover Effects */
.theme-button:hover {
    transform: scale(1.05); /* Slightly enlarge on hover */
    opacity: 0.9; /* Slightly fade on hover */
}

/* Responsive Layout */
.container {
    display: flex;
    flex-wrap: wrap; /* Allows wrapping on smaller screens */
    justify-content: center; /* Center the items */
}

/* Basic responsive styling */
@media (max-width: 600px) {
    p {
        font-size: 16px;
        padding: 15px;
        margin: 20px;
    }

    .theme-button {
        width: 100%; /* Make the button full width on small screens */
    }
}

#main-nav {
    border-bottom: solid rgba(0, 0, 0, .2) 1px;
}

#main-nav > .container {
    align-items: stretch;
    display: flex;
    min-height: 3.25rem;
    width: 100%;
}

.navbar-menu .navbar-end {
    align-items: center;
}

.navbar-item {
    color: #4a4a4a;
    display: block;
    line-height: 1.5;
    padding: 0.5rem 0.75rem;
    position: relative;
}

.navbar-item.is-active {
    color: #205c9a;
}

@media screen and (min-width: 1088px) {
    .navbar, .navbar-end, .navbar-menu {
        flex-grow: 1;
        flex-shrink: 0;
    }
    .navbar, .navbar-end, .navbar-menu {
        align-items: stretch;
        display: flex;
    }
    .navbar-end {
        justify-content: flex-end;
        margin-left: auto;
    }

    .navbar-item {
        align-items: center;
        display: flex;
    }
}

@media screen and (max-width: 768px) {
    #main-nav > .container {
        padding: 0;
    }
    .navbar-menu {
        position: absolute;
        background-color: #fff;
        border-bottom: solid rgba(0, 0, 0, .2) 1px;
        left: 0;
        right: 0;
        top: 77px;
        box-sizing: border-box;
        display: none;
    }
    .navbar-menu.is-active {
        display: block;
    }
    .navbar-brand {
        display: flex;
        flex-grow: 1;
        align-items: center;
    }
    .navbar-brand .navbar-item {
        padding: 0;
    }
    .navbar-burger {
        width: 3rem;
        display: block;
        cursor: pointer;
        margin-left: auto;
    }
    .navbar-burger span {
        height: 2px;
        background-color: #1565c1;
        display: block;
        width: 16px;
        margin-bottom: 4px;
    }
}

.container {
    margin: 0 auto;
    padding-left: 0.75rem;
    padding-right: 0.75rem;
    box-sizing: border-box;
}

@media screen and (min-width: 1472px) {
    .container {
        max-width: 1344px;
    }
}

@media screen and (min-width: 769px) {
    .wrapper {
        display: flex;
    }
}

@media screen and (min-width: 769px) {
    #primary {
        flex: none;
        width: 66.66667%;
        padding-right: 24px;
    }
}

article {
    border-bottom: solid rgba(0, 0, 0, .2) 1px;
    margin-top: 32px;
    padding-bottom: 32px;
}

article .title {
    font-size: 1.5rem;
    font-weight: 600;
    line-height: 2;
}

article .title a {
    color: #363636;
}

article .card-image img {
    min-width: 100%;
}

article .card-content {
    padding-top: 16px;
}

article .card-content .media {
    margin-bottom: 1.5rem;
}

article .post-meta {
    font-size: .75rem;
}

article .post-meta .wrap-posted-on {
    display: block;
    margin-bottom: 8px;
}

aside {
    padding-left: 24px;
    padding-top: 32px;
    border-left: solid rgba(0, 0, 0, .12) 1px;
}

aside {
    border-bottom: solid rgba(0, 0, 0, .12) 1px;
    padding-bottom: 32px;
    margin-bottom: 32px;
}

aside .heading {
    font-size: 1.25rem;
    color: #205c9a;
    margin-bottom: 16px;
}

#recent-posts ul {
    list-style: disc;
    padding-left: 32px;
}

#recent-posts ul li {
    margin-bottom: 16px;
}

#recent-posts ul a {
    color: inherit;
}

footer {
    border-top: 1px solid rgba(0, 0, 0, .2);
    text-align: center;
    font-size: 14px;
    padding: 8px 0;
}

footer a {
    color: inherit;
    margin: 0 4px;
}

.navbar-menu {
    display: none; /* Hide menu by default */
}

.navbar-menu.is-active {
    display: block; /* Show menu when active */
}
V/* General body styling (Dark Mode with uniform gradient background) */
body {
    font-family: 'Roboto', sans-serif; /* Updated font */
    background: linear-gradient(100deg, #000000, #463535, #5a3131, #7e0d0d);
    background-size: cover; /* Ensures the gradient covers the entire viewport */
    background-repeat: no-repeat; /* Prevents any repetition */
    background-attachment: fixed; /* Makes sure gradient stays fixed on scroll */
    color: #e0e0e0; /* Light color for better contrast */
    margin: 0;
    padding: 0;
    transition: background 0.3s, color 0.3s;
}

/* Light Mode Styles */
body.light-mode {
    background: linear-gradient(100deg, #ffffff, #f0f4f8, #d6e8ff, #b3d9ff);
    color: #333; /* Dark text for light mode */
}

/* Header styling */
h1, h2, h3, h4, h5, h6 {
    margin: 20px 0; /* Add margin for spacing */
    padding: 0; /* Reset padding */
    text-align: center; /* Center align */
}

/* Paragraph styling with gradient shading */
p {
    font-size: 18px;
    text-align: center;
    padding: 20px;
    border: none; /* Removed border */
    max-width: 500px;
    margin: 30px auto;
    border-radius: 15px; /* More rounded corners */
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1); /* Subtle shadow */
    transition: background 0.3s, color 0.3s;
}

/* Light Mode Paragraph */
body.light-mode p {
    background: linear-gradient(135deg, #ffffff, #d0dcff, #6bfff8, #00fff2); /* Light gradient */
    color: #555; /* Darker text color for readability */
}

/* Dark Mode Paragraph */
body.dark-mode p {
    background: linear-gradient(135deg, #3b3131, #614040, #7c3a3a, #940a0a); /* Dark gradient */
    color: #fffff4; /* Light text color */
}

/* Toggle Switch Styles */
.switch {
    position: relative;
    display: inline-block;
    width: 50px; /* Wider for easier toggling */
    height: 25px;
    margin: 20px auto; /* Center the switch */
}

.switch input {
    opacity: 0; /* Hide the default checkbox */
    width: 0;
    height: 0;
}

.slider {
    position: absolute;
    cursor: pointer;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-color: #ccc;
    transition: .4s;
    border-radius: 20px; /* Rounded corners */
}

.slider:before {
    position: absolute;
    content: "";
    height: 20px;
    width: 20px;
    left: 2px; /* Adjust for aesthetics */
    bottom: 2px;
    background-color: #fff; /* White toggle */
    transition: .4s;
    border-radius: 50%; /* Round toggle */
}

/* Checkbox checked styles */
input:checked + .slider {
    background-color: #dbcf22; /* Color when checked */
}

input:checked + .slider:before {
    transform: translateX(25px); /* Move toggle to the right */
}

/* Button Styles */
.theme-button {
    font-size: 14px; /* Smaller font size */
    padding: 10px 15px; /* Padding for the button */
    border: none; /* Remove default border */
    border-radius: 20px; /* More rounded corners */
    cursor: pointer; /* Pointer cursor on hover */
    transition: background-color 0.3s, color 0.3s, transform 0.3s; /* Smooth transition */
    font-weight: bold; /* Bold text */
}

/* Light Mode Button */
body.light-mode .theme-button {
    background: linear-gradient(135deg, #e0e0e0, #f4f4f4); /* Light gradient */
    color: #333; /* Dark text color */
}

/* Dark Mode Button */
body.dark-mode .theme-button {
    background: linear-gradient(135deg, #444, #666); /* Dark gradient */
    color: #f4f4f4; /* Light text color */
}

/* Button Hover Effects */
.theme-button:hover {
    transform: scale(1.05); /* Slightly enlarge on hover */
    opacity: 0.9; /* Slightly fade on hover */
}

/* Responsive Layout */
.container {
    display: flex;
    flex-wrap: wrap; /* Allows wrapping on smaller screens */
    justify-content: center; /* Center the items */
}

/* Basic responsive styling */
@media (max-width: 600px) {
    p {
        font-size: 16px;
        padding: 15px;
        margin: 20px;
    }

    .theme-button {
        width: 100%; /* Make the button full width on small screens */
    }
}

#main-nav {
    border-bottom: solid rgba(0, 0, 0, .2) 1px;
}

#main-nav > .container {
    align-items: stretch;
    display: flex;
    min-height: 3.25rem;
    width: 100%;
}

.navbar-menu .navbar-end {
    align-items: center;
}

.navbar-item {
    color: #4a4a4a;
    display: block;
    line-height: 1.5;
    padding: 0.5rem 0.75rem;
    position: relative;
}

.navbar-item.is-active {
    color: #205c9a;
}

@media screen and (min-width: 1088px) {
    .navbar, .navbar-end, .navbar-menu {
        flex-grow: 1;
        flex-shrink: 0;
    }
    .navbar, .navbar-end, .navbar-menu {
        align-items: stretch;
        display: flex;
    }
    .navbar-end {
        justify-content: flex-end;
        margin-left: auto;
    }

    .navbar-item {
        align-items: center;
        display: flex;
    }
}

@media screen and (max-width: 768px) {
    #main-nav > .container {
        padding: 0;
    }
    .navbar-menu {
        position: absolute;
        background-color: #fff;
        border-bottom: solid rgba(0, 0, 0, .2) 1px;
        left: 0;
        right: 0;
        top: 77px;
        box-sizing: border-box;
        display: none;
    }
    .navbar-menu.is-active {
        display: block;
    }
    .navbar-brand {
        display: flex;
        flex-grow: 1;
        align-items: center;
    }
    .navbar-brand .navbar-item {
        padding: 0;
    }
    .navbar-burger {
        width: 3rem;
        display: block;
        cursor: pointer;
        margin-left: auto;
    }
    .navbar-burger span {
        height: 2px;
        background-color: #1565c1;
        display: block;
        width: 16px;
        margin-bottom: 4px;
    }
}

.container {
    margin: 0 auto;
    padding-left: 0.75rem;
    padding-right: 0.75rem;
    box-sizing: border-box;
}

@media screen and (min-width: 1472px) {
    .container {
        max-width: 1344px;
    }
}

@media screen and (min-width: 769px) {
    .wrapper {
        display: flex;
    }
}

@media screen and (min-width: 769px) {
    #primary {
        flex: none;
        width: 66.66667%;
        padding-right: 24px;
    }
}

article {
    border-bottom: solid rgba(0, 0, 0, .2) 1px;
    margin-top: 32px;
    padding-bottom: 32px;
}

article .title {
    font-size: 1.5rem;
    font-weight: 600;
    line-height: 2;
}

article .title a {
    color: #363636;
}

article .card-image img {
    min-width: 100%;
}

article .card-content {
    padding-top: 16px;
}

article .card-content .media {
    margin-bottom: 1.5rem;
}

article .post-meta {
    font-size: .75rem;
}

article .post-meta .wrap-posted-on {
    display: block;
    margin-bottom: 8px;
}

aside {
    padding-left: 24px;
    padding-top: 32px;
    border-left: solid rgba(0, 0, 0, .12) 1px;
}

aside {
    border-bottom: solid rgba(0, 0, 0, .12) 1px;
    padding-bottom: 32px;
    margin-bottom: 32px;
}

aside .heading {
    font-size: 1.25rem;
    color: #205c9a;
    margin-bottom: 16px;
}

#recent-posts ul {
    list-style: disc;
    padding-left: 32px;
}

#recent-posts ul li {
    margin-bottom: 16px;
}

#recent-posts ul a {
    color: inherit;
}

footer {
    border-top: 1px solid rgba(0, 0, 0, .2);
    text-align: center;
    font-size: 14px;
    padding: 8px 0;
}

footer a {
    color: inherit;
    margin: 0 4px;
}

.navbar-menu {
    display: none; /* Hide menu by default */
}

.navbar-menu.is-active {
    display: block; /* Show menu when active */
}
/* General body styling (Dark Mode with uniform gradient background) */
body {
    font-family: 'Roboto', sans-serif; /* Updated font */
    background: linear-gradient(100deg, #000000, #463535, #5a3131, #7e0d0d);
    background-size: cover; /* Ensures the gradient covers the entire viewport */
    background-repeat: no-repeat; /* Prevents any repetition */
    background-attachment: fixed; /* Makes sure gradient stays fixed on scroll */
    color: #e0e0e0; /* Light color for better contrast */
    margin: 0;
    padding: 0;
    transition: background 0.3s, color 0.3s;
}

/* Light Mode Styles */
body.light-mode {
    background: linear-gradient(100deg, #ffffff, #f0f4f8, #d6e8ff, #b3d9ff);
    color: #333; /* Dark text for light mode */
}

/* Header styling */
h1, h2, h3, h4, h5, h6 {
    margin: 20px 0; /* Add margin for spacing */
    padding: 0; /* Reset padding */
    text-align: center; /* Center align */
}

/* Paragraph styling with gradient shading */
p {
    font-size: 18px;
    text-align: center;
    padding: 20px;
    border: none; /* Removed border */
    max-width: 500px;
    margin: 30px auto;
    border-radius: 15px; /* More rounded corners */
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1); /* Subtle shadow */
    transition: background 0.3s, color 0.3s;
}

/* Light Mode Paragraph */
body.light-mode p {
    background: linear-gradient(135deg, #ffffff, #d0dcff, #6bfff8, #00fff2); /* Light gradient */
    color: #555; /* Darker text color for readability */
}

/* Dark Mode Paragraph */
body.dark-mode p {
    background: linear-gradient(135deg, #3b3131, #614040, #7c3a3a, #940a0a); /* Dark gradient */
    color: #fffff4; /* Light text color */
}

/* Toggle Switch Styles */
.switch {
    position: relative;
    display: inline-block;
    width: 50px; /* Wider for easier toggling */
    height: 25px;
    margin: 20px auto; /* Center the switch */
}

.switch input {
    opacity: 0; /* Hide the default checkbox */
    width: 0;
    height: 0;
}

.slider {
    position: absolute;
    cursor: pointer;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-color: #ccc;
    transition: .4s;
    border-radius: 20px; /* Rounded corners */
}

.slider:before {
    position: absolute;
    content: "";
    height: 20px;
    width: 20px;
    left: 2px; /* Adjust for aesthetics */
    bottom: 2px;
    background-color: #fff; /* White toggle */
    transition: .4s;
    border-radius: 50%; /* Round toggle */
}

/* Checkbox checked styles */
input:checked + .slider {
    background-color: #dbcf22; /* Color when checked */
}

input:checked + .slider:before {
    transform: translateX(25px); /* Move toggle to the right */
}

/* Button Styles */
.theme-button {
    font-size: 14px; /* Smaller font size */
    padding: 10px 15px; /* Padding for the button */
    border: none; /* Remove default border */
    border-radius: 20px; /* More rounded corners */
    cursor: pointer; /* Pointer cursor on hover */
    transition: background-color 0.3s, color 0.3s, transform 0.3s; /* Smooth transition */
    font-weight: bold; /* Bold text */
}

/* Light Mode Button */
body.light-mode .theme-button {
    background: linear-gradient(135deg, #e0e0e0, #f4f4f4); /* Light gradient */
    color: #333; /* Dark text color */
}

/* Dark Mode Button */
body.dark-mode .theme-button {
    background: linear-gradient(135deg, #444, #666); /* Dark gradient */
    color: #f4f4f4; /* Light text color */
}

/* Button Hover Effects */
.theme-button:hover {
    transform: scale(1.05); /* Slightly enlarge on hover */
    opacity: 0.9; /* Slightly fade on hover */
}

/* Responsive Layout */
.container {
    display: flex;
    flex-wrap: wrap; /* Allows wrapping on smaller screens */
    justify-content: center; /* Center the items */
}

/* Basic responsive styling */
@media (max-width: 600px) {
    p {
        font-size: 16px;
        padding: 15px;
        margin: 20px;
    }

    .theme-button {
        width: 100%; /* Make the button full width on small screens */
    }
}

#main-nav {
    border-bottom: solid rgba(0, 0, 0, .2) 1px;
}

#main-nav > .container {
    align-items: stretch;
    display: flex;
    min-height: 3.25rem;
    width: 100%;
}

.navbar-menu .navbar-end {
    align-items: center;
}

.navbar-item {
    color: #4a4a4a;
    display: block;
    line-height: 1.5;
    padding: 0.5rem 0.75rem;
    position: relative;
}

.navbar-item.is-active {
    color: #205c9a;
}

@media screen and (min-width: 1088px) {
    .navbar, .navbar-end, .navbar-menu {
        flex-grow: 1;
        flex-shrink: 0;
    }
    .navbar, .navbar-end, .navbar-menu {
        align-items: stretch;
        display: flex;
    }
    .navbar-end {
        justify-content: flex-end;
        margin-left: auto;
    }

    .navbar-item {
        align-items: center;
        display: flex;
    }
}

@media screen and (max-width: 768px) {
    #main-nav > .container {
        padding: 0;
    }
    .navbar-menu {
        position: absolute;
        background-color: #fff;
        border-bottom: solid rgba(0, 0, 0, .2) 1px;
        left: 0;
        right: 0;
        top: 77px;
        box-sizing: border-box;
        display: none;
    }
    .navbar-menu.is-active {
        display: block;
    }
    .navbar-brand {
        display: flex;
        flex-grow: 1;
        align-items: center;
    }
    .navbar-brand .navbar-item {
        padding: 0;
    }
    .navbar-burger {
        width: 3rem;
        display: block;
        cursor: pointer;
        margin-left: auto;
    }
    .navbar-burger span {
        height: 2px;
        background-color: #1565c1;
        display: block;
        width: 16px;
        margin-bottom: 4px;
    }
}

.container {
    margin: 0 auto;
    padding-left: 0.75rem;
    padding-right: 0.75rem;
    box-sizing: border-box;
}

@media screen and (min-width: 1472px) {
    .container {
        max-width: 1344px;
    }
}

@media screen and (min-width: 769px) {
    .wrapper {
        display: flex;
    }
}

@media screen and (min-width: 769px) {
    #primary {
        flex: none;
        width: 66.66667%;
        padding-right: 24px;
    }
}

article {
    border-bottom: solid rgba(0, 0, 0, .2) 1px;
    margin-top: 32px;
    padding-bottom: 32px;
}

article .title {
    font-size: 1.5rem;
    font-weight: 600;
    line-height: 2;
}

article .title a {
    color: #363636;
}

article .card-image img {
    min-width: 100%;
}

article .card-content {
    padding-top: 16px;
}

article .card-content .media {
    margin-bottom: 1.5rem;
}

article .post-meta {
    font-size: .75rem;
}

article .post-meta .wrap-posted-on {
    display: block;
    margin-bottom: 8px;
}

aside {
    padding-left: 24px;
    padding-top: 32px;
    border-left: solid rgba(0, 0, 0, .12) 1px;
}

aside {
    border-bottom: solid rgba(0, 0, 0, .12) 1px;
    padding-bottom: 32px;
    margin-bottom: 32px;
}

aside .heading {
    font-size: 1.25rem;
    color: #205c9a;
    margin-bottom: 16px;
}

#recent-posts ul {
    list-style: disc;
    padding-left: 32px;
}

#recent-posts ul li {
    margin-bottom: 16px;
}

#recent-posts ul a {
    color: inherit;
}

footer {
    border-top: 1px solid rgba(0, 0, 0, .2);
    text-align: center;
    font-size: 14px;
    padding: 8px 0;
}

footer a {
    color: inherit;
    margin: 0 4px;
}

.navbar-menu {
    display: none; /* Hide menu by default */
}

.navbar-menu.is-active {
    display: block; /* Show menu when active */
}
/* General body styling (Dark Mode with uniform gradient background) */
body {
    font-family: 'Roboto', sans-serif; /* Updated font */
    background: linear-gradient(100deg, #000000, #463535, #5a3131, #7e0d0d);
    background-size: cover; /* Ensures the gradient covers the entire viewport */
    background-repeat: no-repeat; /* Prevents any repetition */
    background-attachment: fixed; /* Makes sure gradient stays fixed on scroll */
    color: #e0e0e0; /* Light color for better contrast */
    margin: 0;
    padding: 0;
    transition: background 0.3s, color 0.3s;
}

/* Light Mode Styles */
body.light-mode {
    background: linear-gradient(100deg, #ffffff, #f0f4f8, #d6e8ff, #b3d9ff);
    color: #333; /* Dark text for light mode */
}

/* Header styling */
h1, h2, h3, h4, h5, h6 {
    margin: 20px 0; /* Add margin for spacing */
    padding: 0; /* Reset padding */
    text-align: center; /* Center align */
}

/* Paragraph styling with gradient shading */
p {
    font-size: 18px;
    text-align: center;
    padding: 20px;
    border: none; /* Removed border */
    max-width: 500px;
    margin: 30px auto;
    border-radius: 15px; /* More rounded corners */
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1); /* Subtle shadow */
    transition: background 0.3s, color 0.3s;
}

/* Light Mode Paragraph */
body.light-mode p {
    background: linear-gradient(135deg, #ffffff, #d0dcff, #6bfff8, #00fff2); /* Light gradient */
    color: #555; /* Darker text color for readability */
}

/* Dark Mode Paragraph */
body.dark-mode p {
    background: linear-gradient(135deg, #3b3131, #614040, #7c3a3a, #940a0a); /* Dark gradient */
    color: #fffff4; /* Light text color */
}

/* Toggle Switch Styles */
.switch {
    position: relative;
    display: inline-block;
    width: 50px; /* Wider for easier toggling */
    height: 25px;
    margin: 20px auto; /* Center the switch */
}

.switch input {
    opacity: 0; /* Hide the default checkbox */
    width: 0;
    height: 0;
}

.slider {
    position: absolute;
    cursor: pointer;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-color: #ccc;
    transition: .4s;
    border-radius: 20px; /* Rounded corners */
}

.slider:before {
    position: absolute;
    content: "";
    height: 20px;
    width: 20px;
    left: 2px; /* Adjust for aesthetics */
    bottom: 2px;
    background-color: #fff; /* White toggle */
    transition: .4s;
    border-radius: 50%; /* Round toggle */
}

/* Checkbox checked styles */
input:checked + .slider {
    background-color: #dbcf22; /* Color when checked */
}

input:checked + .slider:before {
    transform: translateX(25px); /* Move toggle to the right */
}

/* Button Styles */
.theme-button {
    font-size: 14px; /* Smaller font size */
    padding: 10px 15px; /* Padding for the button */
    border: none; /* Remove default border */
    border-radius: 20px; /* More rounded corners */
    cursor: pointer; /* Pointer cursor on hover */
    transition: background-color 0.3s, color 0.3s, transform 0.3s; /* Smooth transition */
    font-weight: bold; /* Bold text */
}

/* Light Mode Button */
body.light-mode .theme-button {
    background: linear-gradient(135deg, #e0e0e0, #f4f4f4); /* Light gradient */
    color: #333; /* Dark text color */
}

/* Dark Mode Button */
body.dark-mode .theme-button {
    background: linear-gradient(135deg, #444, #666); /* Dark gradient */
    color: #f4f4f4; /* Light text color */
}

/* Button Hover Effects */
.theme-button:hover {
    transform: scale(1.05); /* Slightly enlarge on hover */
    opacity: 0.9; /* Slightly fade on hover */
}

/* Responsive Layout */
.container {
    display: flex;
    flex-wrap: wrap; /* Allows wrapping on smaller screens */
    justify-content: center; /* Center the items */
}

/* Basic responsive styling */
@media (max-width: 600px) {
    p {
        font-size: 16px;
        padding: 15px;
        margin: 20px;
    }

    .theme-button {
        width: 100%; /* Make the button full width on small screens */
    }
}

#main-nav {
    border-bottom: solid rgba(0, 0, 0, .2) 1px;
}

#main-nav > .container {
    align-items: stretch;
    display: flex;
    min-height: 3.25rem;
    width: 100%;
}

.navbar-menu .navbar-end {
    align-items: center;
}

.navbar-item {
    color: #4a4a4a;
    display: block;
    line-height: 1.5;
    padding: 0.5rem 0.75rem;
    position: relative;
}

.navbar-item.is-active {
    color: #205c9a;
}

@media screen and (min-width: 1088px) {
    .navbar, .navbar-end, .navbar-menu {
        flex-grow: 1;
        flex-shrink: 0;
    }
    .navbar, .navbar-end, .navbar-menu {
        align-items: stretch;
        display: flex;
    }
    .navbar-end {
        justify-content: flex-end;
        margin-left: auto;
    }

    .navbar-item {
        align-items: center;
        display: flex;
    }
}

@media screen and (max-width: 768px) {
    #main-nav > .container {
        padding: 0;
    }
    .navbar-menu {
        position: absolute;
        background-color: #fff;
        border-bottom: solid rgba(0, 0, 0, .2) 1px;
        left: 0;
        right: 0;
        top: 77px;
        box-sizing: border-box;
        display: none;
    }
    .navbar-menu.is-active {
        display: block;
    }
    .navbar-brand {
        display: flex;
        flex-grow: 1;
        align-items: center;
    }
    .navbar-brand .navbar-item {
        padding: 0;
    }
    .navbar-burger {
        width: 3rem;
        display: block;
        cursor: pointer;
        margin-left: auto;
    }
    .navbar-burger span {
        height: 2px;
        background-color: #1565c1;
        display: block;
        width: 16px;
        margin-bottom: 4px;
    }
}

.container {
    margin: 0 auto;
    padding-left: 0.75rem;
    padding-right: 0.75rem;
    box-sizing: border-box;
}

@media screen and (min-width: 1472px) {
    .container {
        max-width: 1344px;
    }
}

@media screen and (min-width: 769px) {
    .wrapper {
        display: flex;
    }
}

@media screen and (min-width: 769px) {
    #primary {
        flex: none;
        width: 66.66667%;
        padding-right: 24px;
    }
}

article {
    border-bottom: solid rgba(0, 0, 0, .2) 1px;
    margin-top: 32px;
    padding-bottom: 32px;
}

article .title {
    font-size: 1.5rem;
    font-weight: 600;
    line-height: 2;
}

article .title a {
    color: #363636;
}

article .card-image img {
    min-width: 100%;
}

article .card-content {
    padding-top: 16px;
}

article .card-content .media {
    margin-bottom: 1.5rem;
}

article .post-meta {
    font-size: .75rem;
}

article .post-meta .wrap-posted-on {
    display: block;
    margin-bottom: 8px;
}

aside {
    padding-left: 24px;
    padding-top: 32px;
    border-left: solid rgba(0, 0, 0, .12) 1px;
}

aside {
    border-bottom: solid rgba(0, 0, 0, .12) 1px;
    padding-bottom: 32px;
    margin-bottom: 32px;
}

aside .heading {
    font-size: 1.25rem;
    color: #205c9a;
    margin-bottom: 16px;
}

#recent-posts ul {
    list-style: disc;
    padding-left: 32px;
}

#recent-posts ul li {
    margin-bottom: 16px;
}

#recent-posts ul a {
    color: inherit;
}

footer {
    border-top: 1px solid rgba(0, 0, 0, .2);
    text-align: center;
    font-size: 14px;
    padding: 8px 0;
}

footer a {
    color: inherit;
    margin: 0 4px;
}

.navbar-menu {
    display: none; /* Hide menu by default */
}

.navbar-menu.is-active {
    display: block; /* Show menu when active */
}
/* General body styling (Dark Mode with uniform gradient background) */
body {
    font-family: 'Roboto', sans-serif; /* Updated font */
    background: linear-gradient(100deg, #000000, #463535, #5a3131, #7e0d0d);
    background-size: cover; /* Ensures the gradient covers the entire viewport */
    background-repeat: no-repeat; /* Prevents any repetition */
    background-attachment: fixed; /* Makes sure gradient stays fixed on scroll */
    color: #e0e0e0; /* Light color for better contrast */
    margin: 0;
    padding: 0;
    transition: background 0.3s, color 0.3s;
}

/* Light Mode Styles */
body.light-mode {
    background: linear-gradient(100deg, #ffffff, #f0f4f8, #d6e8ff, #b3d9ff);
    color: #333; /* Dark text for light mode */
}

/* Header styling */
h1, h2, h3, h4, h5, h6 {
    margin: 20px 0; /* Add margin for spacing */
    padding: 0; /* Reset padding */
    text-align: center; /* Center align */
}

/* Paragraph styling with gradient shading */
p {
    font-size: 18px;
    text-align: center;
    padding: 20px;
    border: none; /* Removed border */
    max-width: 500px;
    margin: 30px auto;
    border-radius: 15px; /* More rounded corners */
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1); /* Subtle shadow */
    transition: background 0.3s, color 0.3s;
}

/* Light Mode Paragraph */
body.light-mode p {
    background: linear-gradient(135deg, #ffffff, #d0dcff, #6bfff8, #00fff2); /* Light gradient */
    color: #555; /* Darker text color for readability */
}

/* Dark Mode Paragraph */
body.dark-mode p {
    background: linear-gradient(135deg, #3b3131, #614040, #7c3a3a, #940a0a); /* Dark gradient */
    color: #fffff4; /* Light text color */
}

/* Toggle Switch Styles */
.switch {
    position: relative;
    display: inline-block;
    width: 50px; /* Wider for easier toggling */
    height: 25px;
    margin: 20px auto; /* Center the switch */
}

.switch input {
    opacity: 0; /* Hide the default checkbox */
    width: 0;
    height: 0;
}

.slider {
    position: absolute;
    cursor: pointer;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-color: #ccc;
    transition: .4s;
    border-radius: 20px; /* Rounded corners */
}

.slider:before {
    position: absolute;
    content: "";
    height: 20px;
    width: 20px;
    left: 2px; /* Adjust for aesthetics */
    bottom: 2px;
    background-color: #fff; /* White toggle */
    transition: .4s;
    border-radius: 50%; /* Round toggle */
}

/* Checkbox checked styles */
input:checked + .slider {
    background-color: #dbcf22; /* Color when checked */
}

input:checked + .slider:before {
    transform: translateX(25px); /* Move toggle to the right */
}

/* Button Styles */
.theme-button {
    font-size: 14px; /* Smaller font size */
    padding: 10px 15px; /* Padding for the button */
    border: none; /* Remove default border */
    border-radius: 20px; /* More rounded corners */
    cursor: pointer; /* Pointer cursor on hover */
    transition: background-color 0.3s, color 0.3s, transform 0.3s; /* Smooth transition */
    font-weight: bold; /* Bold text */
}

/* Light Mode Button */
body.light-mode .theme-button {
    background: linear-gradient(135deg, #e0e0e0, #f4f4f4); /* Light gradient */
    color: #333; /* Dark text color */
}

/* Dark Mode Button */
body.dark-mode .theme-button {
    background: linear-gradient(135deg, #444, #666); /* Dark gradient */
    color: #f4f4f4; /* Light text color */
}

/* Button Hover Effects */
.theme-button:hover {
    transform: scale(1.05); /* Slightly enlarge on hover */
    opacity: 0.9; /* Slightly fade on hover */
}

/* Responsive Layout */
.container {
    display: flex;
    flex-wrap: wrap; /* Allows wrapping on smaller screens */
    justify-content: center; /* Center the items */
}

/* Basic responsive styling */
@media (max-width: 600px) {
    p {
        font-size: 16px;
        padding: 15px;
        margin: 20px;
    }

    .theme-button {
        width: 100%; /* Make the button full width on small screens */
    }
}

#main-nav {
    border-bottom: solid rgba(0, 0, 0, .2) 1px;
}

#main-nav > .container {
    align-items: stretch;
    display: flex;
    min-height: 3.25rem;
    width: 100%;
}

.navbar-menu .navbar-end {
    align-items: center;
}

.navbar-item {
    color: #4a4a4a;
    display: block;
    line-height: 1.5;
    padding: 0.5rem 0.75rem;
    position: relative;
}

.navbar-item.is-active {
    color: #205c9a;
}

@media screen and (min-width: 1088px) {
    .navbar, .navbar-end, .navbar-menu {
        flex-grow: 1;
        flex-shrink: 0;
    }
    .navbar, .navbar-end, .navbar-menu {
        align-items: stretch;
        display: flex;
    }
    .navbar-end {
        justify-content: flex-end;
        margin-left: auto;
    }

    .navbar-item {
        align-items: center;
        display: flex;
    }
}

@media screen and (max-width: 768px) {
    #main-nav > .container {
        padding: 0;
    }
    .navbar-menu {
        position: absolute;
        background-color: #fff;
        border-bottom: solid rgba(0, 0, 0, .2) 1px;
        left: 0;
        right: 0;
        top: 77px;
        box-sizing: border-box;
        display: none;
    }
    .navbar-menu.is-active {
        display: block;
    }
    .navbar-brand {
        display: flex;
        flex-grow: 1;
        align-items: center;
    }
    .navbar-brand .navbar-item {
        padding: 0;
    }
    .navbar-burger {
        width: 3rem;
        display: block;
        cursor: pointer;
        margin-left: auto;
    }
    .navbar-burger span {
        height: 2px;
        background-color: #1565c1;
        display: block;
        width: 16px;
        margin-bottom: 4px;
    }
}

.container {
    margin: 0 auto;
    padding-left: 0.75rem;
    padding-right: 0.75rem;
    box-sizing: border-box;
}

@media screen and (min-width: 1472px) {
    .container {
        max-width: 1344px;
    }
}

@media screen and (min-width: 769px) {
    .wrapper {
        display: flex;
    }
}

@media screen and (min-width: 769px) {
    #primary {
        flex: none;
        width: 66.66667%;
        padding-right: 24px;
    }
}

article {
    border-bottom: solid rgba(0, 0, 0, .2) 1px;
    margin-top: 32px;
    padding-bottom: 32px;
}

article .title {
    font-size: 1.5rem;
    font-weight: 600;
    line-height: 2;
}

article .title a {
    color: #363636;
}

article .card-image img {
    min-width: 100%;
}

article .card-content {
    padding-top: 16px;
}

article .card-content .media {
    margin-bottom: 1.5rem;
}

article .post-meta {
    font-size: .75rem;
}

article .post-meta .wrap-posted-on {
    display: block;
    margin-bottom: 8px;
}

aside {
    padding-left: 24px;
    padding-top: 32px;
    border-left: solid rgba(0, 0, 0, .12) 1px;
}

aside {
    border-bottom: solid rgba(0, 0, 0, .12) 1px;
    padding-bottom: 32px;
    margin-bottom: 32px;
}

aside .heading {
    font-size: 1.25rem;
    color: #205c9a;
    margin-bottom: 16px;
}

#recent-posts ul {
    list-style: disc;
    padding-left: 32px;
}

#recent-posts ul li {
    margin-bottom: 16px;
}

#recent-posts ul a {
    color: inherit;
}

footer {
    border-top: 1px solid rgba(0, 0, 0, .2);
    text-align: center;
    font-size: 14px;
    padding: 8px 0;
}

footer a {
    color: inherit;
    margin: 0 4px;
}

.navbar-menu {
    display: none; /* Hide menu by default */
}

.navbar-menu.is-active {
    display: block; /* Show menu when active */
}
/* General body styling (Dark Mode with uniform gradient background) */
body {
    font-family: 'Roboto', sans-serif; /* Updated font */
    background: linear-gradient(100deg, #000000, #463535, #5a3131, #7e0d0d);
    background-size: cover; /* Ensures the gradient covers the entire viewport */
    background-repeat: no-repeat; /* Prevents any repetition */
    background-attachment: fixed; /* Makes sure gradient stays fixed on scroll */
    color: #e0e0e0; /* Light color for better contrast */
    margin: 0;
    padding: 0;
    transition: background 0.3s, color 0.3s;
}

/* Light Mode Styles */
body.light-mode {
    background: linear-gradient(100deg, #ffffff, #f0f4f8, #d6e8ff, #b3d9ff);
    color: #333; /* Dark text for light mode */
}

/* Header styling */
h1, h2, h3, h4, h5, h6 {
    margin: 20px 0; /* Add margin for spacing */
    padding: 0; /* Reset padding */
    text-align: center; /* Center align */
}

/* Paragraph styling with gradient shading */
p {
    font-size: 18px;
    text-align: center;
    padding: 20px;
    border: none; /* Removed border */
    max-width: 500px;
    margin: 30px auto;
    border-radius: 15px; /* More rounded corners */
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1); /* Subtle shadow */
    transition: background 0.3s, color 0.3s;
}

/* Light Mode Paragraph */
body.light-mode p {
    background: linear-gradient(135deg, #ffffff, #d0dcff, #6bfff8, #00fff2); /* Light gradient */
    color: #555; /* Darker text color for readability */
}

/* Dark Mode Paragraph */
body.dark-mode p {
    background: linear-gradient(135deg, #3b3131, #614040, #7c3a3a, #940a0a); /* Dark gradient */
    color: #fffff4; /* Light text color */
}

/* Toggle Switch Styles */
.switch {
    position: relative;
    display: inline-block;
    width: 50px; /* Wider for easier toggling */
    height: 25px;
    margin: 20px auto; /* Center the switch */
}

.switch input {
    opacity: 0; /* Hide the default checkbox */
    width: 0;
    height: 0;
}

.slider {
    position: absolute;
    cursor: pointer;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-color: #ccc;
    transition: .4s;
    border-radius: 20px; /* Rounded corners */
}

.slider:before {
    position: absolute;
    content: "";
    height: 20px;
    width: 20px;
    left: 2px; /* Adjust for aesthetics */
    bottom: 2px;
    background-color: #fff; /* White toggle */
    transition: .4s;
    border-radius: 50%; /* Round toggle */
}

/* Checkbox checked styles */
input:checked + .slider {
    background-color: #dbcf22; /* Color when checked */
}

input:checked + .slider:before {
    transform: translateX(25px); /* Move toggle to the right */
}

/* Button Styles */
.theme-button {
    font-size: 14px; /* Smaller font size */
    padding: 10px 15px; /* Padding for the button */
    border: none; /* Remove default border */
    border-radius: 20px; /* More rounded corners */
    cursor: pointer; /* Pointer cursor on hover */
    transition: background-color 0.3s, color 0.3s, transform 0.3s; /* Smooth transition */
    font-weight: bold; /* Bold text */
}

/* Light Mode Button */
body.light-mode .theme-button {
    background: linear-gradient(135deg, #e0e0e0, #f4f4f4); /* Light gradient */
    color: #333; /* Dark text color */
}

/* Dark Mode Button */
body.dark-mode .theme-button {
    background: linear-gradient(135deg, #444, #666); /* Dark gradient */
    color: #f4f4f4; /* Light text color */
}

/* Button Hover Effects */
.theme-button:hover {
    transform: scale(1.05); /* Slightly enlarge on hover */
    opacity: 0.9; /* Slightly fade on hover */
}

/* Responsive Layout */
.container {
    display: flex;
    flex-wrap: wrap; /* Allows wrapping on smaller screens */
    justify-content: center; /* Center the items */
}

/* Basic responsive styling */
@media (max-width: 600px) {
    p {
        font-size: 16px;
        padding: 15px;
        margin: 20px;
    }

    .theme-button {
        width: 100%; /* Make the button full width on small screens */
    }
}

#main-nav {
    border-bottom: solid rgba(0, 0, 0, .2) 1px;
}

#main-nav > .container {
    align-items: stretch;
    display: flex;
    min-height: 3.25rem;
    width: 100%;
}

.navbar-menu .navbar-end {
    align-items: center;
}

.navbar-item {
    color: #4a4a4a;
    display: block;
    line-height: 1.5;
    padding: 0.5rem 0.75rem;
    position: relative;
}

.navbar-item.is-active {
    color: #205c9a;
}

@media screen and (min-width: 1088px) {
    .navbar, .navbar-end, .navbar-menu {
        flex-grow: 1;
        flex-shrink: 0;
    }
    .navbar, .navbar-end, .navbar-menu {
        align-items: stretch;
        display: flex;
    }
    .navbar-end {
        justify-content: flex-end;
        margin-left: auto;
    }

    .navbar-item {
        align-items: center;
        display: flex;
    }
}

@media screen and (max-width: 768px) {
    #main-nav > .container {
        padding: 0;
    }
    .navbar-menu {
        position: absolute;
        background-color: #fff;
        border-bottom: solid rgba(0, 0, 0, .2) 1px;
        left: 0;
        right: 0;
        top: 77px;
        box-sizing: border-box;
        display: none;
    }
    .navbar-menu.is-active {
        display: block;
    }
    .navbar-brand {
        display: flex;
        flex-grow: 1;
        align-items: center;
    }
    .navbar-brand .navbar-item {
        padding: 0;
    }
    .navbar-burger {
        width: 3rem;
        display: block;
        cursor: pointer;
        margin-left: auto;
    }
    .navbar-burger span {
        height: 2px;
        background-color: #1565c1;
        display: block;
        width: 16px;
        margin-bottom: 4px;
    }
}

.container {
    margin: 0 auto;
    padding-left: 0.75rem;
    padding-right: 0.75rem;
    box-sizing: border-box;
}

@media screen and (min-width: 1472px) {
    .container {
        max-width: 1344px;
    }
}

@media screen and (min-width: 769px) {
    .wrapper {
        display: flex;
    }
}

@media screen and (min-width: 769px) {
    #primary {
        flex: none;
        width: 66.66667%;
        padding-right: 24px;
    }
}

article {
    border-bottom: solid rgba(0, 0, 0, .2) 1px;
    margin-top: 32px;
    padding-bottom: 32px;
}

article .title {
    font-size: 1.5rem;
    font-weight: 600;
    line-height: 2;
}

article .title a {
    color: #363636;
}

article .card-image img {
    min-width: 100%;
}

article .card-content {
    padding-top: 16px;
}

article .card-content .media {
    margin-bottom: 1.5rem;
}

article .post-meta {
    font-size: .75rem;
}

article .post-meta .wrap-posted-on {
    display: block;
    margin-bottom: 8px;
}

aside {
    padding-left: 24px;
    padding-top: 32px;
    border-left: solid rgba(0, 0, 0, .12) 1px;
}

aside {
    border-bottom: solid rgba(0, 0, 0, .12) 1px;
    padding-bottom: 32px;
    margin-bottom: 32px;
}

aside .heading {
    font-size: 1.25rem;
    color: #205c9a;
    margin-bottom: 16px;
}

#recent-posts ul {
    list-style: disc;
    padding-left: 32px;
}

#recent-posts ul li {
    margin-bottom: 16px;
}

#recent-posts ul a {
    color: inherit;
}

footer {
    border-top: 1px solid rgba(0, 0, 0, .2);
    text-align: center;
    font-size: 14px;
    padding: 8px 0;
}

footer a {
    color: inherit;
    margin: 0 4px;
}

.navbar-menu {
    display: none; /* Hide menu by default */
}

.navbar-menu.is-active {
    display: block; /* Show menu when active */
}
/* General body styling (Dark Mode with uniform gradient background) */
body {
    font-family: 'Roboto', sans-serif; /* Updated font */
    background: linear-gradient(100deg, #000000, #463535, #5a3131, #7e0d0d);
    background-size: cover; /* Ensures the gradient covers the entire viewport */
    background-repeat: no-repeat; /* Prevents any repetition */
    background-attachment: fixed; /* Makes sure gradient stays fixed on scroll */
    color: #e0e0e0; /* Light color for better contrast */
    margin: 0;
    padding: 0;
    transition: background 0.3s, color 0.3s;
}

/* Light Mode Styles */
body.light-mode {
    background: linear-gradient(100deg, #ffffff, #f0f4f8, #d6e8ff, #b3d9ff);
    color: #333; /* Dark text for light mode */
}

/* Header styling */
h1, h2, h3, h4, h5, h6 {
    margin: 20px 0; /* Add margin for spacing */
    padding: 0; /* Reset padding */
    text-align: center; /* Center align */
}

/* Paragraph styling with gradient shading */
p {
    font-size: 18px;
    text-align: center;
    padding: 20px;
    border: none; /* Removed border */
    max-width: 500px;
    margin: 30px auto;
    border-radius: 15px; /* More rounded corners */
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1); /* Subtle shadow */
    transition: background 0.3s, color 0.3s;
}

/* Light Mode Paragraph */
body.light-mode p {
    background: linear-gradient(135deg, #ffffff, #d0dcff, #6bfff8, #00fff2); /* Light gradient */
    color: #555; /* Darker text color for readability */
}

/* Dark Mode Paragraph */
body.dark-mode p {
    background: linear-gradient(135deg, #3b3131, #614040, #7c3a3a, #940a0a); /* Dark gradient */
    color: #fffff4; /* Light text color */
}

/* Toggle Switch Styles */
.switch {
    position: relative;
    display: inline-block;
    width: 50px; /* Wider for easier toggling */
    height: 25px;
    margin: 20px auto; /* Center the switch */
}

.switch input {
    opacity: 0; /* Hide the default checkbox */
    width: 0;
    height: 0;
}

.slider {
    position: absolute;
    cursor: pointer;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-color: #ccc;
    transition: .4s;
    border-radius: 20px; /* Rounded corners */
}

.slider:before {
    position: absolute;
    content: "";
    height: 20px;
    width: 20px;
    left: 2px; /* Adjust for aesthetics */
    bottom: 2px;
    background-color: #fff; /* White toggle */
    transition: .4s;
    border-radius: 50%; /* Round toggle */
}

/* Checkbox checked styles */
input:checked + .slider {
    background-color: #dbcf22; /* Color when checked */
}

input:checked + .slider:before {
    transform: translateX(25px); /* Move toggle to the right */
}

/* Button Styles */
.theme-button {
    font-size: 14px; /* Smaller font size */
    padding: 10px 15px; /* Padding for the button */
    border: none; /* Remove default border */
    border-radius: 20px; /* More rounded corners */
    cursor: pointer; /* Pointer cursor on hover */
    transition: background-color 0.3s, color 0.3s, transform 0.3s; /* Smooth transition */
    font-weight: bold; /* Bold text */
}

/* Light Mode Button */
body.light-mode .theme-button {
    background: linear-gradient(135deg, #e0e0e0, #f4f4f4); /* Light gradient */
    color: #333; /* Dark text color */
}

/* Dark Mode Button */
body.dark-mode .theme-button {
    background: linear-gradient(135deg, #444, #666); /* Dark gradient */
    color: #f4f4f4; /* Light text color */
}

/* Button Hover Effects */
.theme-button:hover {
    transform: scale(1.05); /* Slightly enlarge on hover */
    opacity: 0.9; /* Slightly fade on hover */
}

/* Responsive Layout */
.container {
    display: flex;
    flex-wrap: wrap; /* Allows wrapping on smaller screens */
    justify-content: center; /* Center the items */
}

/* Basic responsive styling */
@media (max-width: 600px) {
    p {
        font-size: 16px;
        padding: 15px;
        margin: 20px;
    }

    .theme-button {
        width: 100%; /* Make the button full width on small screens */
    }
}

#main-nav {
    border-bottom: solid rgba(0, 0, 0, .2) 1px;
}

#main-nav > .container {
    align-items: stretch;
    display: flex;
    min-height: 3.25rem;
    width: 100%;
}

.navbar-menu .navbar-end {
    align-items: center;
}

.navbar-item {
    color: #4a4a4a;
    display: block;
    line-height: 1.5;
    padding: 0.5rem 0.75rem;
    position: relative;
}

.navbar-item.is-active {
    color: #205c9a;
}

@media screen and (min-width: 1088px) {
    .navbar, .navbar-end, .navbar-menu {
        flex-grow: 1;
        flex-shrink: 0;
    }
    .navbar, .navbar-end, .navbar-menu {
        align-items: stretch;
        display: flex;
    }
    .navbar-end {
        justify-content: flex-end;
        margin-left: auto;
    }

    .navbar-item {
        align-items: center;
        display: flex;
    }
}

@media screen and (max-width: 768px) {
    #main-nav > .container {
        padding: 0;
    }
    .navbar-menu {
        position: absolute;
        background-color: #fff;
        border-bottom: solid rgba(0, 0, 0, .2) 1px;
        left: 0;
        right: 0;
        top: 77px;
        box-sizing: border-box;
        display: none;
    }
    .navbar-menu.is-active {
        display: block;
    }
    .navbar-brand {
        display: flex;
        flex-grow: 1;
        align-items: center;
    }
    .navbar-brand .navbar-item {
        padding: 0;
    }
    .navbar-burger {
        width: 3rem;
        display: block;
        cursor: pointer;
        margin-left: auto;
    }
    .navbar-burger span {
        height: 2px;
        background-color: #1565c1;
        display: block;
        width: 16px;
        margin-bottom: 4px;
    }
}

.container {
    margin: 0 auto;
    padding-left: 0.75rem;
    padding-right: 0.75rem;
    box-sizing: border-box;
}

@media screen and (min-width: 1472px) {
    .container {
        max-width: 1344px;
    }
}

@media screen and (min-width: 769px) {
    .wrapper {
        display: flex;
    }
}

@media screen and (min-width: 769px) {
    #primary {
        flex: none;
        width: 66.66667%;
        padding-right: 24px;
    }
}

article {
    border-bottom: solid rgba(0, 0, 0, .2) 1px;
    margin-top: 32px;
    padding-bottom: 32px;
}

article .title {
    font-size: 1.5rem;
    font-weight: 600;
    line-height: 2;
}

article .title a {
    color: #363636;
}

article .card-image img {
    min-width: 100%;
}

article .card-content {
    padding-top: 16px;
}

article .card-content .media {
    margin-bottom: 1.5rem;
}

article .post-meta {
    font-size: .75rem;
}

article .post-meta .wrap-posted-on {
    display: block;
    margin-bottom: 8px;
}

aside {
    padding-left: 24px;
    padding-top: 32px;
    border-left: solid rgba(0, 0, 0, .12) 1px;
}

aside {
    border-bottom: solid rgba(0, 0, 0, .12) 1px;
    padding-bottom: 32px;
    margin-bottom: 32px;
}

aside .heading {
    font-size: 1.25rem;
    color: #205c9a;
    margin-bottom: 16px;
}

#recent-posts ul {
    list-style: disc;
    padding-left: 32px;
}

#recent-posts ul li {
    margin-bottom: 16px;
}

#recent-posts ul a {
    color: inherit;
}

footer {
    border-top: 1px solid rgba(0, 0, 0, .2);
    text-align: center;
    font-size: 14px;
    padding: 8px 0;
}

footer a {
    color: inherit;
    margin: 0 4px;
}

.navbar-menu {
    display: none; /* Hide menu by default */
}

.navbar-menu.is-active {
    display: block; /* Show menu when active */
}
